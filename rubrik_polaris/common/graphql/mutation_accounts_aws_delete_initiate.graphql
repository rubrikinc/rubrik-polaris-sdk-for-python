mutation RubrikPolarisSDKRequest($cloud_account_uuid: UUID!) {
    prepareAwsCloudAccountDeletion(input: {cloudAccountId: $cloud_account_uuid, feature: CLOUD_NATIVE_PROTECTION}) {
        cloudFormationUrl
    }
}