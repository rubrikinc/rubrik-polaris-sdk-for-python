mutation RubrikPolarisSDKRequest($jwt_config: String!, $name: String!)
{
    gcpSetDefaultServiceAccountJwtConfig(
        input: {
            serviceAccountJwtConfig: $jwt_config,
            serviceAccountName: $name
        }
    )
}